import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  AddMealForm: {
    margin: 10,
  },
  LabelInput: {
    marginTop: 20,
  },
  DatePickerWrapper: {
    display: 'flex',
    alignItems: 'center',
    padding: 10,
    marginTop: 20,
  },
});
